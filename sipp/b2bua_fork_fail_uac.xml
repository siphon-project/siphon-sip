<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- A-leg caller for the parallel call.fork() cases that end in a failure
     (fork-allfail and fork-timeout in sipp/b2bua-fork/b2bua_fork.py).

     fork-allfail: the caller gets one final response, and only once the last
     branch has failed. Both branches fail in the 4xx class, 486 at once and 480
     two seconds later, so the 486 is the answer whichever rule within the class
     picks it.

     fork-timeout: one branch is busy at once and the other rings until the 3 s
     ring timeout, which must relay that 486 rather than answer a bare 408.

     Whether each branch was ACKed or CANCELled is asserted by the callee
     scenarios, and that on_failure ran once per case by the CI step reading the
     logs. -->

<scenario name="B2BUA parallel fork caller (all branches fail)">

  <send retrans="500">
    <![CDATA[
INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:caller@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 1 1 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />

  <recv response="486" rtd="true" />

  <send>
    <![CDATA[
ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:caller@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

</scenario>
