<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Hold, resume and a session refresh on a one-legged, engine-anchored call.

  INVITE -> 200 (engine-anchored) -> ACK
         -> re-INVITE a=sendonly  -> 200 + SDP -> ACK      (hold)
         -> re-INVITE a=sendrecv  -> 200 + SDP -> ACK      (resume)
         -> UPDATE                -> 200                   (refresh)
         -> BYE -> 200

  There is no second leg here: siphon answered the call itself and the media
  ends on the engine, which is what an IVR, a queue and a voicemail box all
  look like. Each of these used to fail:

  * the re-INVITE was answered `491 Request Pending` every time, because the
    glare check read the absent B-leg's `initial_acked` as "not yet ACKed". The
    phone backed off, retried, got `491` again, and hold never completed.
  * the UPDATE got a `100 Trying` and then no final response at all, so the
    originator's transaction ran to Timer F. A session-timer refresh sent as an
    UPDATE (RFC 4028 §10) that is never answered ends the call when the
    refresher gives up.

  Both assertions are structural: SIPp fails the scenario on any response it is
  not told to expect, so a `491` or a missing final response fails here rather
  than being read as a pass.
-->
<scenario name="Control-plane hold/resume/refresh on a one-legged call">

  <send retrans="500">
    <![CDATA[
INVITE sip:media@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/control-hold-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" rrs="true">
    <action>
      <ereg regexp="c=IN IP4 203\.0\.113\.1"
            search_in="body"
            check_it="true"
            assign_to="anchored" />
      <log message="answer anchored on the engine: [$anchored]" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <!-- Hold. The engine answers the re-offer, so the 200 carries SDP. -->
  <send retrans="500">
    <![CDATA[
INVITE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
[routes]
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendonly
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true">
    <action>
      <ereg regexp="^v=0"
            search_in="body"
            check_it="true"
            assign_to="hold_answer" />
      <log message="hold answered with SDP, not 491" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <!-- Resume. -->
  <send retrans="500">
    <![CDATA[
INVITE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
[routes]
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687639 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" />

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <!-- Session refresh as an UPDATE (RFC 4028 §10): a final response is owed. -->
  <send retrans="500">
    <![CDATA[
UPDATE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 4 UPDATE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
[routes]
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687640 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550004@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:media@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 5 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
