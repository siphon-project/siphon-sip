<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA re-INVITE UAC scenario:
  INVITE → 200 → ACK → re-INVITE(hold) → 200 → ACK
                      → re-INVITE(resume) → 200 → ACK → BYE → 200

  Tests that the B2BUA correctly forwards mid-dialog re-INVITEs
  (hold/resume) between A-leg and B-leg.
-->
<scenario name="B2BUA re-INVITE UAC">

  <!-- Send initial INVITE to B2BUA -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-reinvite-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Absorb provisionals -->
  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- 200 OK for initial INVITE -->
  <recv response="200" rtd="true" crlf="true" />

  <!-- ACK directly to B2BUA (no routes — B2BUA owns the dialog) -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- re-INVITE #1: HOLD (a=sendonly) -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-reinvite-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendonly
]]>
  </send>

  <!-- Absorb 100 Trying for re-INVITE -->
  <recv response="100" optional="true" />

  <!-- 200 OK for hold re-INVITE -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <!-- RFC 3261 §8.2.6.2: this answers the UAC's own re-INVITE, so its To
           MUST equal the To that re-INVITE carried — `bob` at siphon
           (172.20.0.50), tag and all. The forwarded answer is a clone of the
           B-leg's 200, whose To names the far leg's dialog; the B2BUA swaps the
           dialog tags but used to leave that URI in place, so the UAC saw its
           own hold answered with the B-leg UAS's address. -->
      <ereg regexp="bob@172\.20\.0\.50"
            search_in="hdr" header="To:" check_it="true" assign_to="reinv_to_ok" />
      <!-- The B-leg UAS (172.20.0.52) must not appear in the A-leg dialog
           identity at all — that is the leak itself, not just a cosmetic one. -->
      <ereg regexp="172\.20\.0\.52"
            search_in="hdr" header="To:" check_it_inverse="true"
            assign_to="reinv_to_no_bleg" />
      <ereg regexp="alice@172\.20\.0\.53"
            search_in="hdr" header="From:" check_it="true" assign_to="reinv_from_ok" />
      <log message="re-INVITE 200 OK assertions: to=[$reinv_to_ok] no_bleg=[$reinv_to_no_bleg] from=[$reinv_from_ok]" />
    </action>
  </recv>

  <!-- ACK for hold re-INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- re-INVITE #2: RESUME (a=sendrecv) -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-reinvite-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687639 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Absorb 100 Trying for re-INVITE -->
  <recv response="100" optional="true" />

  <!-- 200 OK for resume re-INVITE -->
  <recv response="200" rtd="true" crlf="true" />

  <!-- ACK for resume re-INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="200" />

  <!-- ============================================================ -->
  <!-- BYE -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 4 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- 200 OK for BYE -->
  <recv response="200" rtd="true" />

</scenario>
