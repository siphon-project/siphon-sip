<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA shutdown-teardown UAC:
  INVITE -> 180 -> 200 OK -> ACK -> (sit) -> receive siphon's BYE -> 200

  The acceptance criterion for the drain teardown, and the one thing a unit test
  cannot reach: it is about signal delivery to a real process. The caller never
  sends a BYE, no session timer and no duration cap are configured, and the ring
  timeout stopped applying at the 200 OK — so the BYE this scenario waits for
  can only be the shutdown pass. Asserting Reason: Q.850;cause=16 is what
  separates it from a timeout teardown, which would carry cause=102.

  Before this existed, a SIGTERM taken with this call up ended at
  "drain timeout — exiting with in-flight work still active": nothing on the
  wire at all, and this scenario would have timed out.
-->
<scenario name="B2BUA shutdown teardown UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-shutdown-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <recv response="200" rtd="true" crlf="true" />

  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Sit on the answered call. siphon must hang it up, not us. -->
  <recv request="BYE" crlf="true">
    <action>
      <!-- RFC 3326: the cause is what says WHY the framework hung up, and it
           is the only thing on the wire that does. Cause 16 is normal clearing
           — an orderly hangup the network chose, which is what an SBC taken out
           of service emits, not a fault for the far side to alarm on. A BYE
           with no Reason would still pass a "did the call end" check. -->
      <ereg regexp="Q\.850;cause=16" search_in="hdr" header="Reason:"
            check_it="true" assign_to="1" />
    </action>
  </recv>

  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

  <!-- $1 exists only to carry check_it; SIPp refuses a scenario with an
       assigned variable nothing references. -->
  <Reference variables="1" />

</scenario>
