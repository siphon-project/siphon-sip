<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Route self-identity UAC scenario (RFC 3261 §16.4).

  INVITE → 180 Ringing → 200 OK → ACK → pause → BYE → 200 OK

  Identical to invite_uac.xml except the Request-URI omits the port. SIPp still
  sends to whatever address:port is given on the command line, so the call is
  driven at siphon's SECOND udp listener while the R-URI stays the registered
  AoR. That matters because a registrar AoR is compared under RFC 3261 §19.1.4
  URI equality: `sip:bob@host` and `sip:bob@host:5070` are NOT the same AoR
  (only the default port is elided), so an R-URI carrying the non-default port
  would miss the binding and fail on the INVITE — before reaching the in-dialog
  behaviour this scenario exists to test.

  `rrs="true"` captures the Record-Route from the 200 OK, and `[routes]` plays
  it back on the ACK and BYE. The final `recv 200` for the BYE is the assertion:
  it only arrives if siphon recognised and consumed the Record-Route it stamped
  itself, then relayed onward.

  Requires a UAS (invite_uas.xml) on the other side of siphon.
-->
<scenario name="Route self-identity UAC">

  <!-- Send INVITE — R-URI without a port, so it matches the registered AoR -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/siphon-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- Capture the route set siphon advertised (rrs) -->
  <recv response="200" rtd="true" crlf="true" rrs="true" />

  <!-- In-dialog ACK: remote target from Contact, Route from Record-Route -->
  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="1000" />

  <!-- The in-dialog request under test: it carries siphon's own Record-Route
       back as a Route header, which siphon must recognise and consume. -->
  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- The assertion: a 200 only comes back if the BYE was relayed. -->
  <recv response="200" rtd="true" />

</scenario>
