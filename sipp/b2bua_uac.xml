<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA UAC scenario:
  INVITE → 180 Ringing → 200 OK → ACK → pause → BYE → 200 OK

  Unlike proxy UAC: no Record-Route/routes, ACK goes directly to B2BUA.
  Requires b2bua_uas.xml on the B-leg side of siphon.
-->
<scenario name="B2BUA call UAC">

  <!-- Send INVITE to the B2BUA -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <!-- Absorb provisionals -->
  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- Expect 200 OK from B2BUA -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <!-- RFC 3891 §6.2 makes this a MUST for any UA that supports Replaces,
           and RFC 5589 §7.3 is why the caller cares: a transferor reads
           `Supported: replaces` off exactly this 200 OK to decide whether it
           can offer an attended transfer. Without it a transferor that gates
           on the tag downgrades to a blind REFER with no Replaces, and its
           own consultation dialog is then left up with nothing to replace it.
           The B2BUA terminates the dialog, so this is siphon's own capability,
           not the B-leg's. -->
      <ereg regexp="replaces"
            search_in="hdr" header="Supported:" check_it="true"
            assign_to="answer_replaces_ok" />
      <log message="B2BUA 200 OK assertions: replaces=[$answer_replaces_ok]" />
    </action>
  </recv>

  <!-- Send ACK directly to B2BUA (no routes — B2BUA owns the dialog) -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Hold call for 1 second -->
  <pause milliseconds="1000" />

  <!-- Send BYE directly to B2BUA -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Expect 200 OK for BYE -->
  <recv response="200" rtd="true" />

</scenario>
