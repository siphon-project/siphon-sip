<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA replace_peer UAC scenario — Alice, the party that SURVIVES:
  INVITE (X-Replace-Peer: carol) → 200 → ACK
  → (siphon dials carol, carol answers, bob is BYE'd)
  → re-INVITE from siphon carrying carol's media → 200 → ACK
  → BYE (carol hangs up, siphon tears this leg down) → 200

  There is NO REFER anywhere in this scenario, in either direction. That is the
  whole point: siphon replaced the callee because the script told it to, and
  Alice's only evidence is that her media got re-pointed mid-call. She is never
  asked to do anything, and never learns who she is talking to now.
-->
<scenario name="B2BUA replace_peer UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-replace-peer-test
X-Replace-Peer: sip:carol@172.20.0.54:5060
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <recv response="200" rtd="true" crlf="true" />

  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!--
    The replacement runs on the answered call: the script arms it from a timer
    (500 ms), siphon dials carol, and only once carol answers is bob BYE'd and
    this leg re-INVITEd. Alice stays connected to bob for the whole of carol's
    ring — that is the behaviour this scenario exists to pin, and the reason
    the verb is not a hangup followed by a re-INVITE.
  -->
  <recv request="INVITE" crlf="true" timeout="20000">
    <action>
      <!-- The re-INVITE must carry an offer: a survivor re-pointed at a new
           party with no SDP would be left sending to the leg that just left. -->
      <ereg regexp="application/sdp"
            search_in="hdr" header="Content-Type:" check_it="true"
            assign_to="reinvite_has_sdp" />
      <!-- RFC 3264 §5 / RFC 4566 §5.2: the B2BUA re-originates the offer under
           its own identity — carol's `o=` owner must not leak to alice. -->
      <ereg regexp="o=carol"
            search_in="body" check_it_inverse="true"
            assign_to="reinvite_no_carol_origin" />
      <log message="replace_peer re-INVITE: sdp=[$reinvite_has_sdp] origin_clean=[$reinvite_no_carol_origin]" />
    </action>
  </recv>

  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
a=sendrecv
]]>
  </send>

  <recv request="ACK" timeout="5000" crlf="true" />

  <!-- Carol hangs up; siphon tears this surviving leg down. -->
  <recv request="BYE" crlf="true" timeout="20000" />

  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

</scenario>
