<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  The script-free control path (`control.inbound`), happy path:
  INVITE -> 100 -> 200 OK (the CONTROLLER's SDP) -> ACK -> BYE -> 200

  On the wire this is identical to control_handover_uac.xml, and that is the
  point: the siphon this dials (172.20.0.176) runs a script with NO
  @b2bua.on_invite in it at all, no media engine and no control listener. The
  only thing that can have routed this call is the `control.inbound` config
  block dialing the application.

  So the body assertion carries the whole claim. The 200 OK must hold the SDP
  the *application* supplied (RFC 5737 TEST-NET-2, 198.51.100.20). A siphon that
  silently fell back to proxy mode, or answered something of its own, would
  either 404 here or complete the call with the wrong body.
-->
<scenario name="Control-plane script-free inbound UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:inbound@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550007@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:inbound@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/control-inbound-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" rrs="true">
    <action>
      <ereg regexp="c=IN IP4 198\.51\.100\.20"
            search_in="body"
            check_it="true"
            assign_to="controller_sdp" />
      <!-- SIPp treats an assigned-but-unreferenced variable as a scenario
           error, so log it. -->
      <log message="200 OK carries the controller's answer SDP: [$controller_sdp]" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550007@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:inbound@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550007@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:inbound@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
