<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  REGISTER with digest authentication (two-step):
  1. Send REGISTER → expect 401 with WWW-Authenticate
  2. Re-send REGISTER with Authorization → expect 200
-->
<scenario name="REGISTER with auth">

  <!-- Step 1: Initial REGISTER (no credentials) -->
  <send retrans="500">
    <![CDATA[
REGISTER sip:[remote_ip] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 REGISTER
Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=3600
Max-Forwards: 70
User-Agent: SIPp/siphon-test
Content-Length: 0

]]>
  </send>

  <!-- Absorb a possible 100 Trying for the REGISTER (siphon's NIST may
       auto-emit one when the registrar takes longer than
       `transaction.auto_100_delay` to respond — typically only at high
       CPS under load).  Optional so it never blocks. -->
  <recv response="100" optional="true" />

  <!-- Expect 401 Unauthorized with challenge -->
  <recv response="401" auth="true" />

  <!-- Step 2: Re-REGISTER with digest credentials -->
  <send retrans="500">
    <![CDATA[
REGISTER sip:[remote_ip] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 2 REGISTER
Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=3600
[authentication]
Max-Forwards: 70
User-Agent: SIPp/siphon-test
Content-Length: 0

]]>
  </send>

  <!-- Same absorber as above — the auth-bearing REGISTER can also race
       a 100 Trying. -->
  <recv response="100" optional="true" />

  <!-- Expect 200 OK -->
  <recv response="200" rtd="true" />

</scenario>
