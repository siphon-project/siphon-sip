<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Places a call whose identities are supplied on the command line, so one
  scenario covers every ETSI TS 103 221-1 target identifier type siphon can
  intercept on.

  The point is *detection*: a warrant is provisioned on one identifier, this
  places a call carrying it, and the Mediation and Delivery Function is then
  asked whether IRI arrived. A warrant that silently matches nothing — the
  failure this whole module exists to prevent — shows up here as an empty
  delivery buffer rather than as a passing test.

  Driven with `-inf li_targets.csv -infindex` or explicit keyword values:

    [from_user]  the From user part      (the calling party)
    [to_uri]  the To / Request-URI    (the called party, a full URI)
    [from_host]  the From host

  Both ends are parameterised because a warrant may name either: the
  originating party (matched off From) or the terminating one (matched off the
  Request-URI and To), and the delivered PDU's direction is defined against
  whichever it is.
-->
<scenario name="LI target UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:[to_uri] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "LI test" <sip:[from_user]@[from_host]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[to_uri]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:[from_user]@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Subject: LI target detection
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="200" rtd="true" />

  <send>
    <![CDATA[
ACK sip:[to_uri] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "LI test" <sip:[from_user]@[from_host]>;tag=[pid]SIPpTag00[call_number]
[last_To:]
Call-ID: [call_id]
CSeq: 1 ACK
Contact: <sip:[from_user]@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Talk for a moment, with real RTP.
       An X3 interception delivers the media the engine relayed, so a call that
       negotiated media but never sent a packet would deliver no content and
       every content assertion would pass vacuously. `play_pcap_audio` streams
       the G.711 sample the sip-tester package ships, which is what makes the
       X3 side of this test mean something. -->
  <nop>
    <action>
      <exec play_pcap_audio="/usr/share/sip-tester/g711a.pcap" />
    </action>
  </nop>

  <pause milliseconds="2000" />

  <send retrans="500">
    <![CDATA[
BYE sip:[to_uri] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "LI test" <sip:[from_user]@[from_host]>;tag=[pid]SIPpTag00[call_number]
[last_To:]
Call-ID: [call_id]
CSeq: 2 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" crlf="true" />

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200" />
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000" />

</scenario>
