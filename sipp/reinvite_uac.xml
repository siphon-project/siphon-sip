<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  RTPEngine re-INVITE UAC scenario:
  INVITE → 200 → ACK → re-INVITE (hold) → 200 → ACK
                      → re-INVITE (resume) → 200 → ACK → BYE → 200

  Tests that RTPEngine offer/answer is called on re-INVITEs (hold/resume),
  not just the initial INVITE.
-->
<scenario name="RTPEngine re-INVITE UAC">

  <!-- Initial INVITE with SDP -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/reinvite-test-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Absorb provisionals -->
  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- 200 OK for initial INVITE — assert it answers CSeq 1, so a
       retransmitted / stale 200 can never satisfy a later recv. -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="1 INVITE" search_in="hdr" header="CSeq:" check_it="true"
            assign_to="invite_cseq_ok" />
      <log message="initial 200 CSeq assertion: [$invite_cseq_ok]" />
    </action>
  </recv>

  <!-- ACK for initial INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <!-- ============================================================ -->
  <!-- re-INVITE #1: HOLD (a=sendonly) -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
User-Agent: SIPp/reinvite-test-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendonly
]]>
  </send>

  <!-- The proxy answers a relayed INVITE with its own 100 Trying
       (RFC 3261 §16.2) — absorb it. -->
  <recv response="100" optional="true" />

  <!-- 200 OK for hold re-INVITE — MUST answer CSeq 2.  Without this check a
       retransmitted 200 for the initial INVITE (CSeq 1) satisfied this recv
       and masked a hold re-INVITE that never reached the callee. -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="2 INVITE" search_in="hdr" header="CSeq:" check_it="true"
            assign_to="hold_cseq_ok" />
      <log message="hold 200 CSeq assertion: [$hold_cseq_ok]" />
    </action>
  </recv>

  <!-- ACK for hold re-INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 ACK
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <!-- ============================================================ -->
  <!-- re-INVITE #2: RESUME (a=sendrecv) -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
User-Agent: SIPp/reinvite-test-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687639 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- The proxy answers a relayed INVITE with its own 100 Trying
       (RFC 3261 §16.2) — absorb it. -->
  <recv response="100" optional="true" />

  <!-- 200 OK for resume re-INVITE — MUST answer CSeq 3.  This is the recv
       that catches the relay-to-self loop: the resume re-INVITE was 482'd
       instead of being forwarded, so no CSeq-3 200 ever arrives. -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <ereg regexp="3 INVITE" search_in="hdr" header="CSeq:" check_it="true"
            assign_to="resume_cseq_ok" />
      <log message="resume 200 CSeq assertion: [$resume_cseq_ok]" />
    </action>
  </recv>

  <!-- ACK for resume re-INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 ACK
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="200" />

  <!-- ============================================================ -->
  <!-- BYE -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 4 BYE
Route: <sip:[remote_ip]:[remote_port];lr>
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- 200 OK for BYE -->
  <recv response="200" rtd="true" />

</scenario>
