<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  In-dialog INFO on a one-legged B2BUA call (RFC 6086):
  INVITE -> 100 -> 200 OK (the controller's SDP) -> ACK
         -> INFO dtmf-relay Signal=5 -> 200
         -> INFO application/dtmf 11 -> 200
         -> BYE -> 200

  The application answers this call itself, so there is no far leg and siphon
  is the party that owes each INFO its response. Before INFO was handled on a
  B2BUA call it took the proxy path and came back 405, so each <recv
  response="200"> below is the SIP half of the acceptance. The other half is
  the application's verdict: both digits must reach it as ChannelDtmfReceived,
  `11` normalised to `#` (RFC 2833 numbering), each exactly once.
-->
<scenario name="Control-plane in-dialog INFO UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:info@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550008@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:info@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/control-info-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" rrs="true">
    <action>
      <ereg regexp="c=IN IP4 198\.51\.100\.20"
            search_in="body"
            check_it="true"
            assign_to="controller_sdp" />
      <!-- SIPp treats an assigned-but-unreferenced variable as a scenario
           error, so log it. -->
      <log message="200 OK carries the controller's answer SDP: [$controller_sdp]" />
      <!-- RFC 3261 §12.1.1: the dialog-establishing 2xx needs a Contact for the
           UAC's [next_url] below, and a To-tag for the in-dialog BYE. -->
      <ereg regexp="Contact:[^\r\n]*sip:"
            search_in="msg"
            check_it="true"
            assign_to="contact_present" />
      <log message="2xx Contact present: [$contact_present]" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550008@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:info@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- The common form: a Signal= / Duration= pair. -->
  <send retrans="500">
    <![CDATA[
INFO [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550008@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:info@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 INFO
[routes]
Max-Forwards: 70
Content-Type: application/dtmf-relay
Content-Length: [len]

Signal=5
Duration=160
]]>
  </send>

  <recv response="200" rtd="true" />

  <!-- The bare form, using RFC 2833's 11 for #, which some devices send. -->
  <send retrans="500">
    <![CDATA[
INFO [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550008@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:info@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 INFO
[routes]
Max-Forwards: 70
Content-Type: application/dtmf
Content-Length: [len]

11
]]>
  </send>

  <recv response="200" rtd="true" />

  <!-- Let the application collect both digits before the call ends. -->
  <pause milliseconds="1500" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550008@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:info@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 4 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
