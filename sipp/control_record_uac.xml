<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Control-plane recording on an engine-anchored channel with no WebSocket bridge:
  INVITE -> 100 -> 200 OK (engine-anchored SDP) -> ACK -> [app records/stops] -> BYE -> 200

  Two things are under test that SIPp can only half see. First, the anchor:
  `call.handover(answer=True, profile=...)` with no ws_uri anchors the A-leg on
  the media engine and lets the engine terminate it, which is the voicemail-box
  and IVR shape — the 200 OK therefore carries the engine's synthesised SDP,
  asserted here on the mock's media address, and not the caller's own c= line
  echoed back. Second, the recording itself, which is entirely invisible from
  here: this scenario only keeps the call up long enough for record_start,
  record_stop and the RecordingFinished event that follows the *closed* file.
  Whether they were performed is asserted on the media engine's recorded
  commands and the control application's verdict (see the CI step).
-->
<scenario name="Control-plane recording UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:record@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550006@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:record@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/control-record-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 101
a=rtpmap:0 PCMU/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" rrs="true">
    <action>
      <ereg regexp="c=IN IP4 203\.0\.113\.1"
            search_in="body"
            check_it="true"
            assign_to="anchored" />
      <log message="answer SDP anchored on the engine with no bridge: [$anchored]" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550006@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:record@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Hold the call while the application records, stops, and waits for the
       RecordingFinished that only arrives once the file is closed. -->
  <pause milliseconds="3000" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550006@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:record@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
