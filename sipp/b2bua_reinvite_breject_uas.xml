<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA B-leg re-INVITE rejected UAS scenario (B-leg side):
  Receive INVITE → 200 → ACK → send re-INVITE → 488 → ACK
                               → receive BYE → 200

  The UAS (B-leg) initiates a re-INVITE which the UAC (A-leg) rejects
  with 488. Tests both Via branch correctness (bug #1) AND that Siphon
  ACKs the 488 forwarded back to the UAS (bug #2) combined from B-leg
  direction.
-->
<scenario name="B2BUA B-leg re-INVITE reject UAS">

  <!-- Receive initial INVITE from B2BUA and capture dialog state -->
  <recv request="INVITE" crlf="true">
    <action>
      <ereg regexp="(.*)" search_in="hdr" header="Call-ID:" assign_to="dialog_callid" />
      <ereg regexp="tag=([^;>\r\n ]+)" search_in="hdr" header="From:" assign_to="junk,remote_tag" />
      <ereg regexp="&lt;([^>]+)>" search_in="hdr" header="From:" assign_to="junk,remote_from_uri" />
      <ereg regexp="&lt;([^>]+)>" search_in="hdr" header="Contact:" assign_to="junk,remote_contact" />
    </action>
  </recv>

  <!-- Send 200 OK with SDP -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Receive ACK from B2BUA -->
  <recv request="ACK" timeout="5000" />

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- UAS-initiated re-INVITE: will be rejected by A-leg with 488 -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE [$remote_contact] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:bob@[local_ip]>;tag=[pid]SIPpTag01[call_number]
To: <[$remote_from_uri]>;tag=[$remote_tag]
Call-ID: [$dialog_callid]
CSeq: 1 INVITE
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-breject-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendonly
]]>
  </send>

  <!-- Absorb 100 Trying -->
  <recv response="100" optional="true" />

  <!-- 488 Not Acceptable Here (forwarded from UAC via B2BUA) -->
  <!-- If Via branch was wrong, B2BUA can't match → no response → timeout -->
  <recv response="488" timeout="5000" />

  <!-- ACK for 488 -->
  <send>
    <![CDATA[
ACK [$remote_contact] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:bob@[local_ip]>;tag=[pid]SIPpTag01[call_number]
To: <[$remote_from_uri]>;tag=[$remote_tag]
Call-ID: [$dialog_callid]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Receive BYE from B2BUA — call must survive after rejected re-INVITE.
       No pause before this: the BYE follows the 488's ACK immediately, and a
       pause here is a window in which SIPp has not yet armed the recv, so an
       on-time BYE is reported as unexpected and the scenario aborts. -->
  <recv request="BYE" />

  <!-- Send 200 OK for BYE -->
  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

</scenario>
