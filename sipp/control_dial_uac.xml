<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  `dial`: the app rings a target that never answers, and the caller is still
  the app's afterwards.

  INVITE -> 100 -> (the app dials a dead target, the ring times out) -> 200 -> ACK -> BYE -> 200

  What this proves is what the caller is doing while the dial fails. Before the
  verb existed, an app could only ring by answering the caller first and
  bridging, or by handing the call back to siphon with `route` — which forwards
  the failure to the caller and ends it. Here the caller sits in ringback for
  the dial's timeout and is then answered by the app, which is "ring the
  extension, and if nobody answers, voicemail".

  The 200 arriving at all is the assertion: it can only come from the app
  answering a call that survived the failed dial. SIPp fails on any response it
  is not told to expect, so a forwarded 408 or 503 fails this scenario rather
  than reading as a pass.
-->
<scenario name="Control-plane dial, caller survives the failure">

  <send retrans="500">
    <![CDATA[
INVITE sip:dial@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550005@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:dial@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:caller@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/control-dial-uac
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
]]>
  </send>

  <recv response="100" optional="true" />

  <!-- No 180 is expected: `dial` relays the B-leg's provisionals, and a target
       that never answers sends none. The wait covers the ring timeout. -->
  <recv response="200" rtd="true" crlf="true" rrs="true" timeout="20000" />

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550005@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:dial@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="300" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550005@control.test>;tag=[pid]SIPpTag00[call_number]
To: <sip:dial@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
