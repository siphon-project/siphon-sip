<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Caller for the over-MTU B2BUA Path scenario.

  Same flow as failover_uac.xml (INVITE, expect 200, ACK, BYE) with the SDP
  padded by a=X-pad attribute lines so the request comfortably exceeds
  listen.mtu - 200 (= 1080 bytes at mtu 1280).  The B-leg the B2BUA derives from
  it is over-MTU too, which puts the RFC 3261 §18.1.1 UDP->TCP re-probe in play.

  The callee (bobmtu) is registered with its Contact host set to the decoy's DNS
  name, which holds a live TCP listener, and its Path pointing at the UDP-only
  edge.  A re-probe that follows the Contact resolves the decoy, finds TCP, and
  delivers the B-leg there — no 200 ever comes back.  A re-probe that follows the
  route set (the URI the destination was actually resolved from) finds no TCP
  path at the edge, stays on UDP, and the call completes.

  Run: sipp <b2bua>:5060 -sf b2bua_path_oversized_uac.xml -s bobmtu -m 1
-->
<scenario name="B2BUA Path over-MTU UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:[service]@[remote_ip] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/siphon-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
a=X-pad:MTU-OVERSIZE-PADDING mtu-oversize-padding MTU-OVERSIZE-PADDING padding
]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />

  <recv response="200" rtd="true" rrs="true" />

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
[last_To:]
[last_Call-ID:]
CSeq: 1 ACK
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[remote_ip]>;tag=[pid]SIPpTag00[call_number]
[last_To:]
[last_Call-ID:]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" />

</scenario>
