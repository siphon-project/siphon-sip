<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA B-leg-initiated re-INVITE UAS scenario (B-leg side):
  Receive INVITE → 200 → ACK → send re-INVITE(hold) → 200 → ACK
                               → receive BYE → 200

  The UAS (B-leg) initiates a re-INVITE after the call is established.
  Tests that the B2BUA correctly forwards UAS-initiated re-INVITEs
  with correct Via branch (targets Via branch bug).
-->
<scenario name="B2BUA B-leg re-INVITE UAS">

  <!-- Receive initial INVITE from B2BUA and capture dialog state -->
  <recv request="INVITE" crlf="true">
    <action>
      <ereg regexp="(.*)" search_in="hdr" header="Call-ID:" assign_to="dialog_callid" />
      <ereg regexp="tag=([^;>\r\n ]+)" search_in="hdr" header="From:" assign_to="junk,remote_tag" />
      <ereg regexp="&lt;([^>]+)>" search_in="hdr" header="From:" assign_to="junk,remote_from_uri" />
      <ereg regexp="&lt;([^>]+)>" search_in="hdr" header="Contact:" assign_to="junk,remote_contact" />
    </action>
  </recv>

  <!-- Send 200 OK with SDP -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Receive ACK from B2BUA -->
  <recv request="ACK" timeout="5000" />

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- UAS-initiated re-INVITE: HOLD (a=sendonly) -->
  <!-- Sent from B-leg back to B2BUA, which should forward to A-leg -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
INVITE [$remote_contact] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:bob@[local_ip]>;tag=[pid]SIPpTag01[call_number]
To: <[$remote_from_uri]>;tag=[$remote_tag]
Call-ID: [$dialog_callid]
CSeq: 1 INVITE
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-bleg-reinvite-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendonly
]]>
  </send>

  <!-- Absorb 100 Trying -->
  <recv response="100" optional="true" />

  <!-- 200 OK for re-INVITE (forwarded from UAC via B2BUA) -->
  <recv response="200" rtd="true" crlf="true" />

  <!-- ACK for re-INVITE 200 OK -->
  <send>
    <![CDATA[
ACK [$remote_contact] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:bob@[local_ip]>;tag=[pid]SIPpTag01[call_number]
To: <[$remote_from_uri]>;tag=[$remote_tag]
Call-ID: [$dialog_callid]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Receive BYE from B2BUA (forwarded from UAC). No pause before it: the UAC
       sends that BYE 200 ms after its own ACK, which is about when this side's
       ACK goes out, so a pause of the same length here ends just as the BYE
       lands, and SIPp aborts a call on a message that arrives mid-pause. The
       recv waits anyway. -->
  <recv request="BYE" />

  <!-- Send 200 OK for BYE -->
  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

</scenario>
