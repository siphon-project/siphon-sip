<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA REFER siphon-terminated — Bob, the surviving far leg:
  Receive INVITE → 180 → 200 → ACK
  → receive the media re-INVITE (siphon re-points Bob's media at the transfer
    target Carol, RFC 3261 §14) → 200 → ACK
  → receive BYE (Carol hung up) → 200

  After the transfer completes, siphon re-INVITEs the surviving party with the
  transfer target's SDP so media flows Bob <-> Carol.
-->
<scenario name="B2BUA REFER terminate Bob UAS">

  <recv request="INVITE" crlf="true" />

  <send>
    <![CDATA[
SIP/2.0 180 Ringing
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Length: 0

]]>
  </send>

  <pause milliseconds="200" />

  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv request="ACK" optional="true" timeout="5000" />

  <!-- siphon re-INVITEs Bob with the transfer target's SDP (media re-anchor). -->
  <recv request="INVITE" crlf="true" />

  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Carol can hang up while this re-INVITE is still waiting for its ACK, so
       the ACK and the forwarded BYE arrive in either order. siphon still ACKs a
       2xx it receives after sending the BYE (RFC 5407 §3.1.3). Remember whether
       the ACK came first, and wait for it after the BYE only if it did not: a
       timed-out wait counts as a failed call, and an ACK landing after the
       scenario ends aborts on a dead call. -->
  <recv request="ACK" optional="true" timeout="5000">
    <action>
      <ereg regexp="ACK" search_in="msg" check_it="false" assign_to="reinvite_acked" />
    </action>
  </recv>

  <recv request="BYE" />

  <send next="call_done" test="reinvite_acked">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

  <recv request="ACK" />

  <label id="call_done" />

  <nop>
    <action>
      <log message="bob: call complete" />
    </action>
  </nop>

</scenario>
