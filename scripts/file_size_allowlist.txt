# Files permitted above the 1,500 production-line threshold.
#
#   <path>  <max production lines>  # why
#
# The number is a ceiling, not a target: the gate fails if a listed file grows
# past its own entry, so an exemption cannot quietly become a licence. Raising a
# number means editing the justification in the same diff, and that edit is the
# review hook. Shrinking a file below 1,500 should remove its entry — the gate
# prints a reminder when that happens.
#
# Two entries below are permanent. The rest each name the split that retires
# them; most are scheduled by the 1.9.0 module split.

# ── Permanent ───────────────────────────────────────────────────────────────
# Data, not logic. Splitting a table costs greppability and buys nothing.
src/diameter/dictionary.rs  2400  # AVP dictionary (TS 29.229/32.299/29.272); a table
src/metrics/mod.rs          2000  # metric registry: one struct field + one registration per metric. +26 for the gateway/registrant provisioning-source pairs (last-success timestamp + failure counter each), +17 for the abandoned-job counter (a pool losing workers to stuck handlers reads identically to a busy one without it), +43 for the registrar backend queue (depth gauge, drop counter, and the two count_aors histograms that split queue wait from backend time), +19 for histogram_summary, which renders a plain Histogram for /admin/metrics.json the way the HistogramVec helper already did (the shared body is factored out, so this is the smaller half of a net-new helper)

# ── Scheduled for the 1.9.0 module split ────────────────────────────────────
src/server/mod.rs            2627  # run_async is one 2,214-line fn; decomposed next, into src/server/**

# ── PyO3 binding layer ──────────────────────────────────────────────────────
# One `#[pymethods]` block per class without the `multiple-pymethods` feature,
# which pulls the `inventory` crate — a new dependency for a navigation win, so
# rejected. Splitting needs the thin-wrapper restructure, scheduled post-1.9.0.
# A large share of each is `///` docstrings, which are the SDK's LLM context and
# are load-bearing rather than padding.
src/script/api/diameter.rs   3540  # single #[pymethods] block; kwargs marshalling per interface. +110 for making all 19 methods awaitable: each grows the owned-argument prep a future needs plus the Python::attach that rebuilds its dict on the way out, and the no-peer arm becomes a resolved coroutine; +76 for the per-method note saying so, which is the docstring a script author and the SDK both read
src/script/api/rtpengine.rs  2800  # same; profile-precedence extraction scheduled post-1.9.0
src/script/api/call.rs       2600  # same; ~40% docstrings
src/script/api/request.rs    2000  # same; +43 for the client-transport accessors and their docs
src/script/api/registrar.rs  1520  # same; PyContact/PyFlow views + the save/save_proxy capture
src/script/api/auth.rs       2490  # UAS digest authenticator moves to src/auth/server.rs post-1.9.0; +49 for the database backend's validate/cache path. +159 for splitting each credential backend into a shared prelude/finish pair with a blocking and an awaitable driver, so the bridge into async wraps only the network call and a decision that never reaches the backend keeps needing no runtime; +157 for the awaitable digest driver and the test harness that calls it from a real event loop, which is the only way an awaitable method can be called at all

# ── Cohesive today, split when next touched ─────────────────────────────────
src/b2bua/actor/store.rs     1600  # one impl CallActorStore over one map; ~100 thin delegations
src/registrar/mod.rs         2250  # one impl Registrar over one store; contact/store split when touched
src/rtpengine/siphon_rtp.rs  2340  # one backend; client/set/events/connection split when touched — control identity + auth handshake now live in siphon_rtp/identity.rs, which took the file back under the previous 2410 budget, so this lowers it rather than leaving the headroom as a silent licence
src/ipsec/mod.rs             2100  # types + impl IpsecManager; manager.rs split when touched
src/script/engine.rs         1569  # ratcheted from 1580: the file watcher and the SIGHUP reloader now live in src/script/watcher.rs. Ratchet down again on the next touch
